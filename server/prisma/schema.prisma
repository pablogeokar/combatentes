generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Partida {
  id        String  @id @default(uuid())
  jogador_1 String
  jogador_2 String?

  @@map("partidas")
}
